# Ecommerce API (Spring Boot)

##  Project Overview
This is a simple RESTful Ecommerce API built using Spring Boot.  
It manages products with basic CRUD operations and in-memory storage (there's no database).

It's Features include:
- Create, read, update, delete products
- Filter products by category, name, and price
- Input validation
- Global exception handling

---

##  Setup Instructions

### 1. Requirements
- Java 25+ (I downloaded with java 26)
- Gradle
- IntelliJ IDEA

### 2. Run the application
Open the terminal in project folder then after opening, run:

```bash
./gradlew bootRun